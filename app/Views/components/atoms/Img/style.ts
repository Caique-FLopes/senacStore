import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  image: {
    width: 250,
    height: 150,
  },
});

export default style;
